<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xingyun.orderpayment.modules.order.mapper.OrderItemMapper">

    <insert id="insertBatch">
        INSERT INTO t_order_item (order_no, product_id, product_name, price, quantity)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderNo}, #{item.productId}, #{item.productName}, #{item.price}, #{item.quantity})
        </foreach>
    </insert>

</mapper>